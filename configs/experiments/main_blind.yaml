experiment: main_blind
protocol_id: unified_blind_protocol_v1
seeds: [0, 1, 2, 3, 4]
problems: [laplace, poisson, burgers_inverse]
pipeline:
  - teacher_pinn
  - dense_bayesian_student_distillation
  - mc_dropout_uncertainty
  - structure_discovery
  - structured_reconstruction
  - guarded_source_selection
  - blind_test_evaluation
guard:
  decision_split: guard_validation
  evaluation_split: blind_test
  forbid_blind_label_access: true
outputs:
  summary: summary_main_blind.csv

